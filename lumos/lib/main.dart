import 'package:flutter/material.dart';

void main() {
  runApp(lumos());
}

class lumos extends StatelessWidget {
  const lumos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Material(
        child: Center(
          child: Container(
            child: Text("nfdfbhb"),
          ),
        ),
      ),
    );
  }
}
